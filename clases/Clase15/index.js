const series = require("./series.js") // modulo propio
const fs = require ("fs") // modulo nativo
const moment = require ("moment")     // modulo de terceros (NPM)

/* const ahora = moment()
console.log(ahora.format("DD-MM-YYYY")); */
